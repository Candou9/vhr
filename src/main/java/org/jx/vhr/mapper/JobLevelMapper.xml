<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jx.vhr.mapper.JobLevelMapper">

    <resultMap type="org.jx.vhr.model.JobLevel" id="JobLevelMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="titleLevel" column="titleLevel" jdbcType="VARCHAR"/>
        <result property="createDate" column="createDate" jdbcType="TIMESTAMP"/>
        <result property="enabled" column="enabled" jdbcType="BIT"/>
    </resultMap>

    <select id="getAllJobLevels" resultMap="JobLevelMap">
        select * from joblevel;
    </select>
    <!--查询单个-->
    <select id="queryById" resultMap="JobLevelMap">
        select id,
               name,
               titleLevel,
               createDate,
               enabled
        from vhr.joblevel
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="JobLevelMap">
        select id,
               name,
               titleLevel,
               createDate,
               enabled
        from vhr.joblevel limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="JobLevelMap">
        select
        id, name, titleLevel, createDate, enabled
        from vhr.joblevel
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="titleLevel != null">
                and titleLevel = #{titleLevel}
            </if>
            <if test="createDate != null">
                and createDate = #{createDate}
            </if>
            <if test="enabled != null">
                and enabled = #{enabled}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into vhr.joblevel(name, titleLevel, createDate, enabled)
        values (#{name}, #{titleLevel}, #{createDate}, #{enabled})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into vhr.joblevel(name, titleLevel, createDate, enabled)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.titleLevel}, #{entity.createDate}, #{entity.enabled})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into vhr.joblevel(name, titleLevel, createDate, enabled)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.titleLevel}, #{entity.createDate}, #{entity.enabled})
        </foreach>
        on duplicate key update
        name = values(name) , titleLevel = values(titleLevel) , createDate = values(createDate) , enabled =
        values(enabled)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update vhr.joblevel
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="titleLevel != null">
                titleLevel = #{titleLevel},
            </if>
            <if test="createDate != null">
                createDate = #{createDate},
            </if>
            <if test="enabled != null">
                enabled = #{enabled},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from vhr.joblevel
        where id = #{id}
    </delete>

    <delete id="deleteJobLevelByIds">
        delete from joblevel where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
</mapper>

